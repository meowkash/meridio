const { ActionSheetIOS } = require("react-native");
const { act } = require("react-test-renderer");

const UserPreferencesReducer = (state, action) => {
    switch (action.type) {
        case 'VISIBILITY_CHANGED': 

        case 'ACCENT_COLOR_CHANGED':
            
    }
}